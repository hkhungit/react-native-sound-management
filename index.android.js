import React from 'react';
import {AppRegistry} from 'react-native';
import ReactNativeSoundManagement from './app/Media';

AppRegistry.registerComponent('ReactNativeSoundManagement', () => ReactNativeSoundManagement);
